// src/views/pages/memberships/Memberships.js

import React from 'react'

const Memberships = () => {
  return (
    <div style={{ padding: '2rem' }}>
      <h1>Memberships</h1>
      <p>This page is only visible to logged-in users.</p>
      <p>
        Here, you could display membership plans, allow the user to purchase a subscription,
        or show their current membership status, etc.
      </p>
    </div>
  )
}

export default Memberships
